[package]
name = "rmq"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.2"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "my_benchmark"
harness = false

[[bench]]
name = "scaling"
harness = false
